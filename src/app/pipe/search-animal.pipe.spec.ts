import { SearchAnimalPipe } from './search-animal.pipe';

describe('SearchAnimalPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchAnimalPipe();
    expect(pipe).toBeTruthy();
  });
});
